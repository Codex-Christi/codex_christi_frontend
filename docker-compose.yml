services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: codex-con-app-start
    env_file:
      - .env.production
    command: ['node', 'server.js']
    ports:
      - '3000:3000'
    restart: always
    networks:
      - webserver-network

volumes:
  my_volume: {}

networks:
  webserver-network:
    external: true
    name: webserver-network
